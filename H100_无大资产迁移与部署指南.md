# BACE H100 无大资产迁移与部署指南

> **状态提示（2026-08-26）：旧 RWTH/H100 部署参考。** 其中的绝对路径、Slurm 流程和环境假设不应直接复制到新设备。当前交接入口为 [`docs/README.md`](docs/README.md)。

## 1. 推荐结论

当前阶段推荐直接迁移工作目录，不推荐先上传 GitHub 再 clone。

原因：

- `verl-agent-src` 当前包含大量 modified 和 untracked BACE 文件，普通 clone 无法恢复这些内容；
- `BACE-work`、`BACE-work-2`、`deploy`、顶层文档等不属于 `verl-agent-src` 的 Git 仓库；
- 当前需要迁移的是已经在 A100 上通过 LoRA tiny smoke 的完整工作状态，而不仅是某个 Git commit；
- 模型和 ALFWorld 数据不适合直接放入普通 GitHub 仓库。

可以暂时不迁移以下两个大目录：

```text
assets/models/                 约 2.9 GB
assets/alfworld/               约 2.3 GB
```

但必须保留：

```text
assets/training_data/alfworld_text_16_64/train.parquet
assets/training_data/alfworld_text_16_64/test.parquet
assets/README_RUNTIME_ASSETS.md
```

这部分只有约 20 KB，启动脚本依赖它。不要排除整个 `assets/`。

## 2. 为什么不建议直接拖拽散文件

直接拖拽目录可以工作，但存在以下风险：

- GUI 工具可能忽略隐藏的 `verl-agent-src/.git`；
- Windows 中转可能改变 shell 脚本换行符或丢失 executable 权限；
- 传输中断后不容易判断缺少了哪些小文件；
- 目录包含大量小文件，逐文件传输通常比单个归档慢；
- 无法方便地做整体 SHA256 校验。

推荐先在源机器创建一个不含大资产的 tar 包，然后把单个 tar 包下载到本地电脑，再上传到 H100 机器。本地电脑不需要解压。

## 3. 源机器打包

确认没有正在向项目目录写入的训练任务：

```bash
ps -ef | grep -E 'verl.trainer.main_ppo|run_bace_alfworld' | grep -v grep
```

记录当前代码状态：

```bash
cd /mnt/xyr/ICLR2027-work/work-BACE-3

git -C verl-agent-src rev-parse HEAD > TRANSFER_GIT_HEAD.txt
git -C verl-agent-src status --short --branch > TRANSFER_GIT_STATUS.txt
git -C verl-agent-src diff --binary > TRANSFER_WORKTREE.patch
```

在项目的上一级目录打包，只排除大模型和完整 ALFWorld 数据：

```bash
cd /mnt/xyr/ICLR2027-work

tar -cf work-BACE-3-no-large-assets.tar \
  --exclude='work-BACE-3/assets/models' \
  --exclude='work-BACE-3/assets/alfworld' \
  work-BACE-3

sha256sum work-BACE-3-no-large-assets.tar \
  > work-BACE-3-no-large-assets.tar.sha256

ls -lh work-BACE-3-no-large-assets.tar*
```

预计归档大小约 150--250 MB，具体取决于实验输出。

如果必须使用 FAT32 U 盘或传输平台有单文件限制，可以切分：

```bash
split -b 500M -d -a 3 \
  work-BACE-3-no-large-assets.tar \
  work-BACE-3-no-large-assets.tar.part-

sha256sum work-BACE-3-no-large-assets.tar.part-* \
  > work-BACE-3-no-large-assets.parts.sha256
```

## 4. 本地电脑中转

下载以下文件：

```text
work-BACE-3-no-large-assets.tar
work-BACE-3-no-large-assets.tar.sha256
```

不要在本地电脑解压、重新压缩或用文本编辑器打开其中的脚本。

如果本地电脑有 Linux、macOS 或 Windows WSL，可校验：

```bash
sha256sum -c work-BACE-3-no-large-assets.tar.sha256
```

Windows PowerShell 可以使用：

```powershell
Get-FileHash .\work-BACE-3-no-large-assets.tar -Algorithm SHA256
```

然后把 tar 和 sha256 文件上传到 H100 服务器的临时目录。

## 5. H100 服务器解包

本文假定最终目录仍为：

```text
/mnt/xyr/ICLR2027-work/work-BACE-3
```

先校验归档。以下 `/path/to/upload` 替换为实际上传目录：

```bash
cd /path/to/upload
sha256sum -c work-BACE-3-no-large-assets.tar.sha256
```

确保目标位置没有另一个同名目录，避免新旧文件混合。然后解包：

```bash
mkdir -p /mnt/xyr/ICLR2027-work

tar -xf /path/to/upload/work-BACE-3-no-large-assets.tar \
  -C /mnt/xyr/ICLR2027-work

cd /mnt/xyr/ICLR2027-work/work-BACE-3
```

检查关键文件：

```bash
test -d verl-agent-src/.git
test -d verl-agent-src/recipe/bace_gigpo
test -d verl-agent-src/tests/bace_gigpo
test -f verl-agent-src/examples/gigpo_trainer/run_bace_alfworld_gpu.sh
test -f assets/training_data/alfworld_text_16_64/train.parquet
test -f assets/training_data/alfworld_text_16_64/test.parquet

git -C verl-agent-src rev-parse HEAD
git -C verl-agent-src status --short --branch
```

如果 shell 脚本的 executable 权限在中转过程中丢失，恢复权限：

```bash
chmod +x deploy/gpu/*.sh
chmod +x verl-agent-src/examples/gigpo_trainer/run_bace_alfworld_*.sh
```

不要运行 `git reset`、`git clean` 或用远端仓库覆盖当前工作树。

## 6. H100 主机前置检查

```bash
uname -m
nvidia-smi
nvidia-smi topo -m
df -h
free -h
```

需要确认：

- Linux x86_64；
- GPU 确实为 H100，确认是 80 GB、94 GB 还是 MIG 实例；
- NVIDIA Driver 能支持 PyTorch CUDA 12.8 wheel；
- 有可用的 Conda/Miniforge；
- 有 Python package index、Hugging Face 和 ALFWorld 下载网络；
- 训练前明确选择空闲 GPU，不抢占其他用户进程。

不要复制 A100 机器上的 Conda 环境。FlashAttention 等二进制组件必须在 H100 机器重新安装或编译。

## 7. 创建 verl-agent 基础环境

加载 H100 机器上的 Conda：

```bash
source "$(conda info --base)/etc/profile.d/conda.sh"
```

创建 Python 3.12 环境，只使用 conda-forge，避免默认 Anaconda channel 的 TOS 问题：

```bash
conda create -n verl-agent \
  -c conda-forge --override-channels \
  python=3.12 pip -y

conda activate verl-agent
```

按照当前 verl-agent upstream 基线安装 vLLM：

```bash
python -m pip install vllm==0.11.0
```

安装 CUDA 12.8 编译工具：

```bash
conda install \
  -c nvidia -c conda-forge --override-channels \
  cuda-nvcc=12.8 -y
```

在 H100 上重新编译 FlashAttention。H100 使用 SM90，不要沿用 A100 的 SM80：

```bash
export CUDA_HOME="$CONDA_PREFIX"
export PATH="$CUDA_HOME/bin:$PATH"
export TORCH_CUDA_ARCH_LIST="9.0"
export MAX_JOBS=8

python -m pip install \
  flash-attn==2.7.4.post1 \
  --no-build-isolation \
  --no-cache-dir
```

FlashAttention 源码编译可能需要几十分钟。

安装当前工作树：

```bash
cd /mnt/xyr/ICLR2027-work/work-BACE-3/verl-agent-src
python -m pip install -e .
```

## 8. 创建独立 ALFWorld 环境并下载资产

项目提供的脚本会：

1. 将 `verl-agent` 克隆为 `verl-agent-alfworld`；
2. 安装 ALFWorld、Gymnasium、Stable-Baselines3 和 TensorBoard；
3. 下载模型与 ALFWorld 数据；
4. 校验 Python 包和资产。

执行：

```bash
cd /mnt/xyr/ICLR2027-work/work-BACE-3
bash deploy/gpu/setup_alfworld_and_assets.sh
```

默认下载位置：

```text
assets/models/Qwen2.5-1.5B-Instruct/
assets/alfworld/
```

如果 H100 服务器不能访问 Hugging Face 或 ALFWorld 下载源，需要稍后单独传输这两个资产目录，不能继续真实 smoke。

环境创建后重新设置 CUDA 路径，避免继承基础环境的旧前缀：

```bash
conda activate verl-agent-alfworld
export CUDA_HOME="$CONDA_PREFIX"
export PATH="$CUDA_HOME/bin:$PATH"
export TORCH_CUDA_ARCH_LIST="9.0"
```

## 9. 安装与资产验证

```bash
cd /mnt/xyr/ICLR2027-work/work-BACE-3

python - <<'PY'
import alfworld
import flash_attn
import torch
import vllm
import verl

print("torch:", torch.__version__)
print("torch CUDA:", torch.version.cuda)
print("CUDA available:", torch.cuda.is_available())
print("GPU count:", torch.cuda.device_count())
print("vLLM:", vllm.__version__)
print("FlashAttention:", flash_attn.__version__)
PY

test -f assets/models/Qwen2.5-1.5B-Instruct/model.safetensors
test -f assets/alfworld/logic/alfred.pddl
test -f assets/training_data/alfworld_text_16_64/train.parquet
```

选择一张确认空闲的 H100，然后运行预检：

```bash
nvidia-smi

CUDA_VISIBLE_DEVICES=0 GPU_COUNT=1 \
  bash deploy/gpu/check_gpu_environment.sh
```

此处的 GPU 编号必须替换为实际获准使用的空闲卡。

## 10. H100 smoke 顺序

### 10.1 第一阶段：LoRA tiny smoke

先复现 A100 上已经成功的最小路径：

```bash
CUDA_VISIBLE_DEVICES=0 \
BACE_EXP_ROOT=/mnt/xyr/ICLR2027-work/work-BACE-3/experiments/h100-tiny \
bash verl-agent-src/examples/gigpo_trainer/run_bace_alfworld_a100_tiny_smoke.sh \
  actor_rollout_ref.model.lora_rank=8 \
  actor_rollout_ref.model.lora_alpha=8
```

脚本名称中有 `a100`，但它是通用 CUDA 单卡 tiny 配置，可以用于 H100。

成功条件：

```text
train_exit_code=0
trace_validation_exit_code=0
Training Progress: 100%
trace_validation JSON: ok=true
artifact summary: status=complete
```

### 10.2 第二阶段：B=8 dense 单步 smoke

LoRA tiny 通过后，再验证完整 dense actor update 和 B=8 语义：

```bash
CUDA_VISIBLE_DEVICES=0 GPU_COUNT=1 \
TRAIN_DATA_SIZE=1 VAL_DATA_SIZE=1 \
ENV_MAX_STEPS=4 MAX_RESPONSE_LENGTH=128 \
ACTOR_MICRO_BATCH=1 LOGPROB_MICRO_BATCH=1 PPO_MINI_BATCH=8 \
GPU_MEMORY_UTILIZATION=0.35 \
BACE_EXP_ROOT=/mnt/xyr/ICLR2027-work/work-BACE-3/experiments/h100-dense-smoke \
bash verl-agent-src/examples/gigpo_trainer/run_bace_alfworld_gpu_smoke.sh \
  trainer.total_training_steps=1
```

### 10.3 第三阶段：标准 H100 smoke

前两步通过后，才运行标准入口：

```bash
CUDA_VISIBLE_DEVICES=0 GPU_COUNT=1 \
BACE_EXP_ROOT=/mnt/xyr/ICLR2027-work/work-BACE-3/experiments/h100-smoke \
bash verl-agent-src/examples/gigpo_trainer/run_bace_alfworld_gpu_smoke.sh
```

标准 smoke 稳定后，再考虑多卡和完整训练。

## 11. 结果检查

每次运行检查：

```text
run_metadata/<run_name>/run_metadata.txt
run_metadata/<run_name>/resolved_command.sh
run_metadata/<run_name>/gpu_samples.csv
bace_artifacts/<run_name>/step_*/summary.json
trace_validation/<run_name>.json
rollout_trajectories/<run_name>/
```

重点确认：

- `trainer.device=cuda`；
- H100 型号和可见卡数正确；
- train exit code 为 0；
- trace validation 为 `ok=true`；
- `R + Q = B`；
- `requested = validated`；
- 没有 silent dropped leaves；
- 没有 CUDA OOM、NCCL error 或 Ray worker 异常退出。

## 12. 当前已知边界

- A100 上已成功的是 LoRA rank 8、1 training step、B=2、max_steps=2 的 tiny smoke；
- A100 dense actor 在与其他用户共享 GPU 时于 Adam optimizer step OOM，这不是代码逻辑错误；
- tiny smoke 没有覆盖完整 branch/Replay，因为极小预算下最终 branches 为 0；
- H100 上仍需完成 B=8 dense smoke，才能认为主训练路径在目标设备上跑通；
- H100 的显存、吞吐、NCCL 和 Tensor Parallel 参数必须重新测量，不能照搬 A100 结果；
- 不要运行旧 NPU/HCCL 脚本；GPU 主入口是 `run_bace_alfworld_gpu*.sh`。

## 13. 何时考虑 GitHub

GitHub 更适合后续代码治理，不适合本次第一次原样迁移。后续如果要使用 GitHub：

1. 先审计全部 modified/untracked 文件；
2. 确认没有模型、数据、凭据、日志或机器路径被提交；
3. 将顶层目录整理成明确的仓库结构；
4. 使用私有仓库；
5. 大模型和 ALFWorld 数据继续放在对象存储或独立资产目录；
6. 提交后在另一目录做一次全新 clone，并验证能够重建当前功能。

在完成这些工作之前，本次迁移以 tar + SHA256 为准。
